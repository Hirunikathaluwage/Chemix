// import User from "../models/user.js";

// export const getPendingPrescriptions = async (req, res) => {
//   const prescriptions = await prescriptions
//     .find({ status: "PENDING" })
//     .populate("user", "name email");

//   res.json(prescriptions);
// };

// export const reviewPrescriptions = async => {
//     const {id} = req.params;
//     const {medicines, comments }
// }
